# Agent、大模型上下文、Skill三者概念关系

1. 大模型的上下文，是Agent工作的基础环境。上下文窗口限定了Agent能够读取的历史信息长度；上下文内容质量，直接决定Agent推理、工具调用的效果。上下文过小，Agent会遗忘历史任务，无法完成复杂工作。

2. Skill是沉淀下来的可复用任务知识模板。Agent可以加载Skill，直接复用一套固定工作流程，不用每次重新写全部提示词。

3. 逻辑链条：

Skill（固化任务流程） → 交给Agent执行 → Agent依靠大模型上下文保存对话、任务信息，完成任务。
