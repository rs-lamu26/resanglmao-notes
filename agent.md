# Agent（智能体）学习资料

## 一、概念个人解释

我自己的理解：Agent 就是"一个会自己决定下一步干什么的大模型"。

普通的聊天 AI，你问一句它答一句，答完就结束了，它不会自己去找资料、不会自己动手改文件。
Agent 不一样，它被给了一堆工具（读文件、搜索网页、执行命令），然后自己循环做三件事：

> 看看现在什么情况 → 想一想下一步该干啥 → 调用一个工具去干 → 看看干得咋样 → 再想下一步……

一直循环，直到它觉得任务完成了，或者干不动了才停。

所以判断一个东西是不是 Agent，我只看一条：**下一步是模型自己决定的，还是人/代码提前写死的？**
如果是提前写死一串固定步骤，那叫工作流（Workflow），不叫 Agent。Anthropic 官方也是这么区分的：
工作流是"LLM 和工具按照预先写好的代码路径被编排"，Agent 是"LLM 自己动态规划流程和工具使用"。

一句话版本：**Workflow 是照着菜谱做菜，Agent 是打开冰箱自己决定做什么菜。**

## 二、核心机制与组成

一个能跑起来的 Agent，基本由四块拼起来：

1. **大模型（大脑）**：负责判断、规划、决定调用哪个工具。没有它，剩下三块都是死的。
2. **工具（手）**：搜索、读文件、写文件、跑命令、调接口。工具是 Agent 能"改变外部世界"的唯一途径。
3. **上下文/记忆（记事本）**：把之前的对话、工具返回的结果、已经做过的事全部攒在一起，下一步决策全靠它。这就是"大模型的上下文"。
4. **循环控制（什么时候停）**：模型自己判断任务是否完成，或者设置最大步数兜底，防止它无限打转。

运行流程（经典循环）：

```
用户给目标
   ↓
模型读上下文 → 决定调用某个工具（比如 read_file）
   ↓
工具执行，把结果塞回上下文
   ↓
模型再读一遍上下文 → 判断：继续调工具 / 输出结果结束
   ↓
（循环，直到完成或达到步数上限）
```

Anthropic 把这种结构叫 "augmented LLM"（增强过的大模型）：大模型 + 检索 + 工具 + 记忆。
另外官方强调：真正跑得好的 Agent 通常不复杂，都是简单可组合的模式拼出来的，而不是套一个巨大的框架。

## 三、实际应用场景

**场景：用 Agent 自动整理一个作业仓库。**

比如我这次的作业：老师要求仓库里必须有 SKILL.md、三份学习资料、README、.gitignore，层级还不能错。

如果没有 Agent，我得自己一个一个建文件夹、改后缀名、写内容、再 git 提交，十几步全靠手。
交给 Agent 之后，我只需要说一句"按这个要求把仓库建好"，它会：

1. 先看一眼仓库现在有什么（读目录）；
2. 对比要求，发现缺 `.workbuddy/skills/concept-learn-skill/`；
3. 自己 `mkdir -p` 建目录、写入 SKILL.md；
4. 生成三份学习资料，检查板块是不是齐全；
5. 建 README 和 .gitignore；
6. 最后 `git add / commit / push`。

中间每一步的结果它都能看到，错了会自己改。这就是 Agent 比"一段提示词"强的地方：**它能在过程中根据真实反馈调整，而不是一次性赌一个答案。**

## 四、易混淆问题 & 使用边界

**容易混的四个东西：**

| 概念 | 谁决定下一步 | 适合什么情况 |
| --- | --- | --- |
| 普通聊天 | 用户一句一句问 | 问答、写作 |
| 工作流 Workflow | 代码提前写死路径 | 步骤固定、要求稳定的任务（如固定格式报表） |
| Agent | 模型临时决定 | 步骤没法提前确定、需要临场判断的任务 |
| 脚本/程序 | 人写死全部逻辑 | 完全确定、不允许有任何偏差的任务 |

**什么时候不该用 Agent：**

- **任务路径很明确**：能写死流程就写死，工作流的成功率通常比 Agent 高、也更好测。Anthropic 官方原话是"找最简单的方案，只有在必要时才增加复杂度"。
- **要求百分百准确、不允许出错**：比如转账、发正式邮件、删文件，Agent 会自己发挥，风险高，改成工作流 + 人工确认更稳。
- **成本敏感**：Agent 一次任务要跑很多轮模型调用，token 消耗是单次问答的几十倍。
- **错误无法回滚**：优先用确定性代码，别让模型自己乱动不可逆的东西。

**一个误区**：Agent 不等于"更聪明"。同一个模型，套上 Agent 循环只是让它能多试几次、多用几个工具；模型本身的理解能力没变。所以模型看不懂的东西，做成 Agent 照样看不懂。

## 五、参考资料来源

1. Anthropic 官方文章《Building effective agents》（讲 Workflow 和 Agent 的区别、什么时候该用）：
   https://www.anthropic.com/research/building-effective-agents
2. Anthropic 官方课程《Workflows vs agents》（对比两种架构的优缺点）：
   https://academy.claude.com/courses/building-with-the-claude-api/workflows-vs-agents
3. Model Context Protocol 官方站（Agent 调用外部工具的开放协议）：
   https://modelcontextprotocol.io/
4. WorkBuddy（腾讯）官方文档 - 产品简介（看 Agent 类工具在实际办公场景里的落地形态）：
   https://www.workbuddy.cn/docs/workbuddy/Overview
